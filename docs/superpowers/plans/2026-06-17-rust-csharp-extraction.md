# Rust C# extraction parity plan

- [x] Add a failing CLI parity test for C# `callers`, `callees`, and `impact`.
- [x] Route C# through the existing C-family fallback extractor.
- [x] Run focused and regression checks.
- [x] Commit and push the migration slice.
