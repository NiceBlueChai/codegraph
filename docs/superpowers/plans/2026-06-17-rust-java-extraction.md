# Rust Java extraction parity plan

- [x] Add a failing CLI parity test for Java `callers`, `callees`, and `impact`.
- [x] Route Java through the existing C-family fallback extractor.
- [x] Run focused and regression checks.
- [x] Commit and push the migration slice.
