# Rust Go extraction parity plan

- [x] Add a failing CLI parity test for Go `callers`, `callees`, and `impact`.
- [x] Implement Go fallback extraction for functions, methods, and simple call references.
- [x] Run the focused test, query parity tests, MCP daemon tests, library tests, `cargo check`, and diff checks.
- [x] Commit and push the migration slice.
